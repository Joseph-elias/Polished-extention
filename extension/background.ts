// Background script for Polished (service worker)
// Currently not used for persistent logic, but required by manifest

chrome.runtime.onInstalled.addListener(() => {
  // Future: analytics, context menu, etc.
});
